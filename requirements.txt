# --- Wajib untuk service (Fase A, mode stub) ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # parsing multipart/form-data (upload foto)
pillow>=10.0              # decode & validasi gambar
python-dotenv>=1.0        # baca file .env (STUB_MODE, ML_ALLOWED_ORIGINS, dst)

# --- Wajib untuk melatih & inferensi model asli (Fase B) ---
# Boleh belum di-install saat cuma menguji integrasi pakai STUB_MODE=true —
# app/predictor.py sengaja TIDAK meng-import torch selama mode stub.
torch>=2.2
torchvision>=0.17
pandas>=2.0              # baca labels.csv
scikit-learn>=1.3        # split stratified + confusion matrix
